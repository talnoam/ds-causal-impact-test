services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    environment:
      - DATABASE_PATH=/app/data/shop.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
